# is of the format
# protocol://username:password@host:port/database
# disable ssl mode for local development by adding the query: ?sslmode=disable
DB_URL=""
PLATFORM="dev" # "dev" or "prod"
JWT_SECRET="" # use `openssl rand -base64 64` to generate a secret
POLKA_KEY=""
